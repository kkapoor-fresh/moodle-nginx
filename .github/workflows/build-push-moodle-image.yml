name: 🔨 Build and publish Moodle images to Artifactory

# concurrency:
#   group: ${{ github.ref }}
#   cancel-in-progress: true




env:
  APP: moodle
  USER: ${{ github.actor }}
  MOODLE_DOCKER_FILE_PATH: ./Moodle.Dockerfile
  

on:
  workflow_call:
# on:
#   push:
#     branches:
#       - dev
#       #- test
#       #- prod
#     paths:
#       - 'Moodle.Dockerfile'
#       - '**/config/moodle/**'
#       - '**/workflows/build-push-moodle-image.yml'
#   pull_request:
#     branches:
#       - dev
#       #- test
#       #- prod
#     paths:
#       - 'Moodle.Dockerfile'
#       - '**/config/moodle/**'
#       - '**/workflows/build-push-moodle-image.yml'
jobs:
  #Print variables for logging and debugging purposes
  checkEnv:
    environment: dev
    name: 📋 Environment Check
    runs-on: ubuntu-latest
    steps:
      - name: Check Env Vars
        run: |
          echo Building ${{ env.APP }}:${{ vars.OPENSHIFT_DEPLOY_PROJECT }}-${{ github.ref_name }}

  # setEnv:
  #   name: 📋 Environment Check
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Import env file
  #       id: dotenv
  #       uses: ./.github/actions/dotenv-action


         



  #     - name: Set Env Vars
  #       run: |
  #         echo "Import .env to $GITHUB_ENV (ignoring any commented [#] or empty lines)"
  #         grep -v '^#' ./example.env | grep -v '^$' >> $GITHUB_ENV
  #         grep -v '^#' ./versions.env | grep -v '^$' >> $GITHUB_ENV
  
      # - name: Print ENV
      #   run: |
      #     echo "PHP image is: "${{ steps.dotenv.outputs.PHP_IMAGE }}
  

  # Build Images

  build-images:

    name: '🔨 Build Moodle image and push to Artifactory'
    needs: [checkEnv]
    runs-on: ubuntu-latest
    environment: dev
  # if: ${{ needs.checkEnv.SKIP_BUILDS != 'YES' }} && (github.ref_name == 'dev' || github.ref_name == 'test' || github.ref_name == 'prod')
    if: ${{ vars.SKIP_BUILD!= 'YES' }} && (github.ref_name == 'dev' || github.ref_name == 'test' || github.ref_name == 'prod')
    steps:
      # Checkout the PR branch
      - name: 📤 Checkout Target Branch
        uses: actions/checkout@v2

      - name: Import env file
        id: dotenv
        uses: dotenv-action



      # Login to Artifactory
      - name: 🔑 Login to Artifactory
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.ARTIFACTORY_URL }}
          username: ${{ secrets.ARTIFACTORY_USER }}
          password: ${{ secrets.ARTIFACTORY_PASSWORD }}

      - name: 🛠️ Pull PHP base image
        run: |
          docker pull ${{ steps.dotenv.outputs.PHP_IMAGE }}
          docker tag ${{ steps.dotenv.outputs.PHP_IMAGE }} ${{ secrets.ARTIFACTORY_URL }}/${{ steps.dotenv.outputs.PHP_IMAGE }}
          docker push ${{ secrets.ARTIFACTORY_URL }}/${{steps.dotenv.outputs.PHP_IMAGE}}

      - name: 🛠️ Build Moodle Image and push to Artifactory
        run: docker buildx build -t ${{ secrets.ARTIFACTORY_URL }}/${{ env.APP }}:${{ vars.OPENSHIFT_DEPLOY_PROJECT }}-${{ github.ref_name }} --output=type=image,push=true --push -f ${{ env.MOODLE_DOCKER_FILE_PATH }} .

