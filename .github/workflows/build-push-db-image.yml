name: 🔨 Build and publish DB image to Artifactory

# concurrency:
#   group: ${{ github.ref }}
#   cancel-in-progress: true

env:
  APP: moodle
  USER: ${{ github.actor }}

on:
  workflow_call:

# on:
#   push:
#     branches:
#       - dev
#       #- test
#       #- prod
#     paths:
#       - '**/config/mariadb/**'
#       - '**/workflows/build-push-db-image.yml'
#   pull_request:
#     branches:
#       - dev
#       #- test
#       #- prod
#     paths:
#       - '**/config/mariadb/**'
#       - '**/workflows/build-push-db-image.yml'
jobs:
  #Print variables for logging and debugging purposes
  checkEnv:
    name: 📋 Environment Check
    runs-on: ubuntu-latest
    steps:
      - name: Check Env Vars
        run: |
          echo Pulling ${{env.DB_IMAGE}}
          echo Push to ${{ secrets.ARTIFACTORY_URL }}/${{env.DB_IMAGE}}

  # # Build Images

  # build-images:
  #   name: '🔨 Build DB image'
  #   needs: [checkEnv]
  #   runs-on: ubuntu-latest
  #   if: ${{ needs.checkEnv.SKIP_BUILDS != 'YES' }} && (github.ref_name == 'dev' || github.ref_name == 'test' || github.ref_name == 'prod')
  #   steps:
  #     # Login to Artifactory
  #     - name: 🔑 Login to Artifactory
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ${{ secrets.ARTIFACTORY_URL }}
  #         username: ${{ secrets.ARTIFACTORY_USER }}
  #         password: ${{ secrets.ARTIFACTORY_PASSWORD }}

  #     - name: 🛠️ Pull latest DB base image, tag, then push to Artifactory
  #       run: |
  #         docker pull ${{env.DB_IMAGE}}
  #         docker tag ${{env.DB_IMAGE}} ${{ secrets.ARTIFACTORY_URL }}/${{env.DB_IMAGE}}
  #         docker push ${{ secrets.ARTIFACTORY_URL }}/${{env.DB_IMAGE}}
